### Distribution optimizer
## Before running the application make sure to install all the required libraries using the below command
    pip install -r templates/requirement.txt
## To run the application, go to 'codes' directory and run the below command
    streamlit run app.py

