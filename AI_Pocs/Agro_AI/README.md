# Procedure to run the application

## Install all the required packages. 
  Run the requirements.txt file.
  Navigate to 'templates' directory and execute the below command in terminal.
  
    pip install -r requirement.txt

## Start the application.
  Navigate to 'codes' directory and execute the below command in terminal.

    streamlit run app.py

### Note:
    To test the application, login and choose the section and upload the corresponding images.